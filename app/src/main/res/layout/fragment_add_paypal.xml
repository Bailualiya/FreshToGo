<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:orientation="vertical"
    android:background="#F8FAFC"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:padding="24dp"
    android:gravity="center_horizontal">

    <ImageView
        android:layout_width="64dp"
        android:layout_height="64dp"
        android:src="@drawable/paypal"
        android:scaleType="fitCenter"
        android:adjustViewBounds="true"
        android:layout_marginBottom="16dp"
        android:contentDescription="@string/paypal_icon_description" />

    <TextView
        android:text="@string/link_paypal_account"
        android:textSize="20sp"
        android:textStyle="bold"
        android:layout_marginBottom="16dp"
        android:textColor="#1F2937"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content" />

    <EditText
        android:id="@+id/paypal_email_input"
        android:hint="@string/paypal_email"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:inputType="textEmailAddress"
        android:layout_marginBottom="24dp"
        android:background="@android:drawable/edit_text"
        android:padding="12dp"
        android:textSize="16sp"
        android:autofillHints="emailAddress" />

    <Button
        android:id="@+id/save_paypal_button"
        android:text="@string/link_paypal"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:backgroundTint="#22C55E"
        android:textColor="#FFFFFF"
        android:textSize="16sp"
        android:padding="12dp" />

    <Button
        android:id="@+id/cancel_paypal_button"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="@string/cancel"
        android:textSize="16sp"
        android:layout_marginTop="12dp"
        android:backgroundTint="#E5E7EB"
        android:textColor="#1F2937" />
</LinearLayout>
